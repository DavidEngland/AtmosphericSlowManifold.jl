Dick hits on the two classic vulnerabilities of stable boundary layer (SBL) diagnostics: coarse vertical discretization artificially inflating $Ri$ and internal gravity waves masquerading as turbulent mixing. The 5-level detailed profile dataset (prof_file_all6_ed_hd.txt) gives you the exact high-order turbulence parameters needed in AtmosphericSlowManifold.jl to address these critiques directly.

1. Quantify the Vertical Spacing ($\Delta z$) Artifact
Because $Ri \propto \frac{\partial \theta}{\partial z} \Big/ \left(\frac{\partial U}{\partial z}\right)^2$, coarse vertical spacing smooths out localized shear layers, dropping the squared shear denominator and artificially spiking calculated $Ri$ (the Fernando & Weil 2010 effect).

* Multi-scale $Ri$ Calculation: Compute local $Ri$ for tight adjacent pairs (e.g., $z_2 - z_1$) versus wide spans (e.g., $z_5 - z_1$).
* Sensitivity Mapping: Plot calculated $Ri$ as a function of grid gap $\Delta z$. Demonstrate whether your fold framework's critical $Ri_c$ stabilizes or systematically shifts as $\Delta z \to 0$.
2. Disambiguate Turbulence from Wave Activity
McNider suspects that high-$Ri$ points are non-turbulent wave motions or wind-meandering rather than active mixing. Filter the SHEBA dataset using sonic anemometer statistics:

* Turbulence Thresholding: Cross-reference high-$Ri$ points against local friction velocity ($u_*1 \dots u_*5$) and vertical velocity variance ($\sigma_w$, mapped to sgw1 \dots sgw5).
* Structure Functions: Check temperature and velocity structure parameters ($C_T^2, C_w^2$ via cT21 \dots cT25, cw21 \dots cw25). Active turbulence maintains inertial subrange signatures; collapsing $C_w^2$ alongside elevated $Ri$ signals non-turbulent wave activity.
* Quality Filtering: Retain only records where FFT quality flags fl1 \dots fl5 == 0.
3. 1D Fine-Grid Synthetic Coarsening
Use AtmosphericSlowManifold.jl to run a high-resolution 1D column model ($\Delta z \approx 0.1\text{ m}$). Subsample and coarsen the synthetic model output to the exact sensor heights ($z_1 \dots z_5$) of SHEBA and CASES-99 to prove whether the fold structure is a physical feature of the slow manifold or an observational aggregation artifact.

SHEBA Profile Data Mapping Key

Parameter	SHEBA Columns	Julia Analysis Role
Heights & Shear	z1–z5, ws1–ws5	Measure vertical shear $\frac{\partial U}{\partial z}$ across variable $\Delta z$
Stratification	T1–T5, Press	Compute potential temperature gradient $\frac{\partial \theta}{\partial z}$
Turbulence Signals	u*1–u*5, sgw1–sgw5	Isolate active mechanical mixing from wave motion
Inertial Subrange	cw21–cw25, cT21–cT25	Validate energy cascade ($C_w^2, C_T^2$) under high stability
Addressing these three angles will directly address Dick's concerns and provide solid justification for your fold framework.

—

Integrating wavelet-based wave-turbulence decomposition directly addresses Dick's core critique—proving that high-$Ri$ states reflect active manifold dynamics rather than passive wave variance. Continuous Morlet wavelets give you the exact time-frequency resolution needed to isolate intermittent turbulence bursts from non-transporting gravity wave packets in raw 20 Hz sonic data.

Preprocessing & Wavelet Pipeline Architecture

* Continuous Morlet Wavelet Transform (CWT): Apply CWT to 20 Hz $u, v, w, T$ time series. Morlet wavelets preserve phase information, making them ideal for detecting localized, non-stationary wave packets versus coherent turbulent eddies.
* Cospectral Gap Identification: Identify the dynamic scale-dependent minimum in the real part of the cross-wavelet spectrum (cospectra of $w'\theta'$ and $u'w'$). Scales larger than the gap represent non-transporting wave motion or wind-meandering; smaller scales represent active turbulent flux.
* Variance Partitioning: Separate total variance into turbulent kinetic energy ($e_{turb}$) and wave kinetic energy ($e_{wave}$). Recompute gradient Richardson numbers using wave-filtered turbulent scales to produce a stabilized $Ri_{clean}$.
Julia Workflow (AtmosphericSlowManifold.jl)

Analysis Stage	Tool / Method	Operational Goal
Data Ingestion	DSP.jl / Custom I/O	Load 20 Hz sonic anemometer series ($u, v, w, T$) from CASES-99 / SHEBA
Multiscale CWT	ContinuousWavelets.jl	Run Morlet transform to construct scale-dependent cross-power spectra
Gap Extraction	Cospectral Gap Algorithm	Partition scalar and momentum fluxes where cospectral density drops to zero
Fold Sharpening	Manifold Diagnostic Engine	Re-plot $Ri_{clean}$ against non-dimensional gradients to isolate kinematic fold "knees"
Key Diagnostic Outcomes

* $Ri$ Noise Stabilization: Removing $e_{wave}$ eliminates spurious high-$Ri$ spikes caused by non-mixing orbital wave velocities.
* Fold Sharpening: Stripping wave variance removes the blurring around transition "knees," clarifying whether the fold structure represents a true physical bifurcation on the slow manifold.
* Automated Quality Control: Integrates cleanly with SHEBA's FFT quality flags (fl1–fl5), providing a reproducible, automated QC pipeline before feeding profiles into your manifold solver.
Would you like to start by sketching the CWT cospectral gap extraction module in Julia, or focus on setting up the pipeline to ingest the CASES-99 20 Hz dataset first?

—

Your SpectralBL and SpectralBL-Analytics frameworks provide the precise mathematical machinery needed to address Dick McNider's concerns. By leveraging Gegenbauer/Chebyshev expansions and high-order $p$-FEM across both tower profiles and 1D Single Column Model (SCM) runs, you can directly separate true physical manifold folds from numerical discretization artifacts.

Eliminating the Fernando & Weil $\Delta z$ Artifact
Sparse finite-differencing across 5 tower levels ($z_1 \dots z_5$) smooths out velocity profiles, underestimating squared shear $\left(\frac{\partial U}{\partial z}\right)^2$ and artificially inflating calculated $Ri$.

* Analytic Gradient Reconstruction: Fitting discrete tower data using Chebyshev or Gegenbauer series (SpectralBL-Analytics) yields continuous, analytical derivatives $\frac{\partial U}{\partial z}$ and $\frac{\partial \theta}{\partial z}$, stripping away grid-spacing inflation.
* Gegenbauer Parameter Tuning ($\alpha$): Adjusting the ultraspherical weight parameter $\alpha$ in $(1-x^2)^{\alpha - 1/2}$ allows the expansion to tightly fit extreme surface-layer gradients without introducing Runge phenomenon or artificial ringing.
Fine-Grid SCM & Synthetic Subsampling
Dick specifically requested a fine-grid 1D model to test whether the elevated $Ri_c$ is real. Running an SCM in SpectralBL provides the ideal synthetic testbed.

* High-$p$ Ground Truth: Generate a high-resolution, low-dispersion benchmark SCM profile using $p$-FEM.
* Synthetic Tower Filtering: Subsample the continuous SCM state at the exact 5 observational heights of SHEBA ($z_1 \dots z_5$).
* Discretization Error Mapping: Compare $Ri$ computed via coarse finite differences against the true $Ri$ from high-$p$ spectral evaluation. This quantitatively demonstrates how much $Ri$ expansion stems from profile coarsening versus true physical fold dynamics.
Dispersion-Free Wave Resolution
Standard low-order finite-difference SCMs suffer from numerical diffusion, which artificially damps internal gravity waves or smears transition knees. The high polynomial degree $p$ in $p$-FEM minimizes numerical dissipation and phase error, allowing internal waves to propagate cleanly without corrupting the slow manifold solution.
McNider's Concern	SpectralBL / SpectralBL-Analytics Solution
Observation spacing inflates $Ri$	Project 5-level data onto Gegenbauer/Chebyshev basis; compute analytic gradients to eliminate $\Delta z$ finite-difference errors.
Missing fine-scale shear layers	Gauss-Lobatto nodal clustering naturally concentrates resolution near $z \to 0$, capturing thin shear zones.
Need fine-grid 1D modeling	Execute $p$-FEM SCM simulations to establish grid-independent benchmark solutions.
Wave vs. turbulence ambiguity	Low-dispersion $p$-FEM preserves wave phase speeds, preventing numerical wave smearing into diagnosed turbulent mixing.

---

This $2 \times 2$ factorial architecture transforms the manuscript defense into an airtight, falsifiable proof of fold existence. Correcting the assumption on $Ri$ bias is crucial: because $Ri_g^{(\Delta z)}$ depends on the spatial covariance $\operatorname{Cov}(\Delta \theta, \Delta U)$, finite differencing can distort gradients in either direction depending on local layer shear.

**The $2 \times 2$ Factorial Validation Matrix**

| Grid / Wave Matrix | Raw Signal | Flux-Filtered ($Co_{w\theta}$) |
| --- | --- | --- |
| **Fine Grid ($p$-FEM SCM)** | **State A** (Physical Ground Truth) | **State B** (Pure Turbulence Manifold) |
| **5-Level Geometry ($\mathcal{H}_{\text{SHEBA}}$)** | **State C** (Discretization Artifact) | **State D** (Observed Target State) |

* **Wave Contamination Effect:** $\Vert{}A - B\Vert{}$
* **Vertical Resolution Bias:** $\Vert{}A - C\Vert{}$
* **Residual Diagnostic Error:** $\mathcal{E}_{\text{obs}} = D - B$

**Flux-Based Cospectral Gap Classification**

Replacing simple variance ($\sigma_w^2$) with normalized cospectral coherence $G(s) = \frac{\vert{}Co_{w\theta}(s)\vert{}}{\sqrt{P_w(s) P_\theta(s)}}$ directly addresses whether high-$Ri$ states perform physical mixing.

* **Wave-Dominated Modes:** High variance power $P_w(s)$ accompanied by vanishing transport $Co_{w\theta}(s) \approx 0$ and $G(s) < G_{\text{crit}}$.
* **Turbulent Modes:** Coherent scale-dependent transport where cross-spectral density $Co_{w\theta}(s)$ maintains phase alignment and $G(s) \ge G_{\text{crit}}$.
* **Indeterminate Regimes:** Explicitly categorized records failing quality control or displaying non-stationary scale overlap, preventing forced misclassification into binary bins.

**`AtmosphericSlowManifold.jl` Implementation Pipeline**

* **Observation Operator ($\mathcal{H}_{\text{SHEBA}}$):** Maps continuous high-order SCM solutions $q(z,t) = \{U, V, \theta, e\}_{\text{fine}}$ onto discrete sensor heights $z_1 \dots z_5$.
* **Asymptotic Resolution Test:** Computes critical Richardson profiles $Ri_c(\Delta z)$ across refinement steps $\Delta z \in \{5.0, 2.0, 1.0, 0.5, 0.2, 0.1\}\text{ m}$. Physical folds require convergence to a non-zero limit:

$$\lim_{\Delta z \to 0} Ri_c(\Delta z) = Ri_c^*$$


* **Manifold Invariant Survival:** Verifies whether geometric manifold features—specifically Jacobian singularities $\det J_y \to 0$, curvature thresholds $\chi \to \chi_c$, and minimal eigenvalues $\lambda_{\min} \to 0$—persist under the action of $\mathcal{H}_{\text{SHEBA}}[q]$.

If fold metrics survive both wave filtering ($A \to B$) and the observation operator ($\mathcal{H}_{\text{SHEBA}}$), the SBL knee is mathematically proven to be a true dynamical fold on the atmospheric slow manifold rather than an observational artifact.

---

Morlet wavelet transforms are utilized as a rigorous diagnostic tool for wave-turbulence separation in high-frequency observational data. They are specifically employed to address the challenge of internal gravity waves masquerading as turbulent mixing in the very stable boundary layer (vSBL).
1. Role in Wave-Turbulence Separation
The primary function of the Morlet wavelet transform is to isolate intermittent turbulence bursts from non-transporting gravity wave packets. Because wave variance can falsely inflate measurements of turbulent kinetic energy (\(e\)) and the Richardson number (\(Ri\)), this separation is critical for accurately identifying the point where turbulent equilibrium ceases to exist—the fold locus (\(C_{fold}\)).
2. Technical Advantages
Morlet wavelets are preferred in this architecture for several reasons:
* Time-Frequency Resolution: They provide the exact resolution needed to detect localized, non-stationary wave packets versus coherent turbulent eddies in raw 20 Hz sonic data.
* Phase Preservation: Morlet wavelets preserve phase information, which is essential for distinguishing between transporting turbulent fluxes and non-transporting oscillatory wave motions.
* Multiscale Analysis: The framework implements a Multiscale Continuous Wavelet Transform (CWT) (often via the ContinuousWavelets.jl package) to construct scale-dependent cross-power spectra for \(u, v, w,\) and \(T\).
3. Preprocessing Pipeline Architecture
The wavelet-based pipeline (specifically identified as Test 3 using CASES-99 data) follows a structured sequence:
* CWT Application: The transform is applied to high-frequency time series to construct scale-dependent spectra.
* Cospectral Gap Identification: The algorithm identifies the dynamic minimum in the real part of the cross-wavelet spectrum (e.g., \(w'\theta'\) and \(u'w'\)). Scales larger than this gap are classified as wave motion, while smaller scales represent active turbulent flux.
* Variance Partitioning: Total variance is partitioned into turbulent kinetic energy (\(e_{turb}\)) and wave kinetic energy (\(e_{wave}\)).
* Noise Stabilization: Removing \(e_{wave}\) eliminates spurious high-\(Ri\) spikes caused by non-mixing orbital wave velocities, producing a stabilized \(Ri_{clean}\).
4. Key Scientific Outcomes
The application of Morlet wavelets leads to Fold Sharpening. By stripping away wave variance that blurs transition "knees," the framework clarifies whether a profile structure represents a true physical bifurcation on the atmospheric slow manifold. Additionally, wavelet detrending and quality control (QC) settings are established as standard campaign-specific metadata to ensure the reproducibility of these transitions across different field sites.

---

The integration of continuous Morlet wavelets into `AtmosphericSlowManifold.jl` bridges 20 Hz raw sensor signals directly to the $2 \times 2$ validation matrix, operationalizing $Ri_{\text{clean}}$ and sharpening the fold locus ($C_{\text{fold}}$).

**Mathematical Formulation of CWT Cospectra**

The complex Morlet wavelet family $\psi_0(\eta) = \pi^{-1/4} e^{i \omega_0 \eta} e^{-\eta^2/2}$ (with nondimensional frequency $\omega_0 = 6$) provides optimal joint time-frequency localization. For vertical velocity $w(t)$ and potential temperature $\theta(t)$, the continuous scale-dependent cross-wavelet spectrum is defined as:

$$W_{w\theta}(s, t) = \mathcal{W}_w(s, t) \cdot \mathcal{W}_\theta^*(s, t)$$

where $\mathcal{W}_x(s, t)$ is the Continuous Wavelet Transform (CWT) at scale $s$ and time $t$. The dynamic cospectrum is isolated via the real part:

$$Co_{w\theta}(s, t) = \operatorname{Re}\left[ W_{w\theta}(s, t) \right]$$

**Pipeline Pipeline & Module Architecture in Julia**

* **Wavelet Decomposition (`WaveletPreprocessing.jl`):** Executes multiscale CWT using `ContinuousWavelets.jl` on 20 Hz sonic time series to build scale-dependent power and cross-power matrices.
* **Dynamic Scale-Gap Extraction:** Evaluates normalized coherence $G(s, t) = \frac{\vert{}Co_{w\theta}(s, t)\vert{}}{\sqrt{P_w(s, t) P_\theta(s, t)}}$. Searches for the local scale minimum $s_{\text{gap}}$ where $G(s_{\text{gap}}, t) < G_{\text{crit}}$, establishing a non-arbitrary boundary between turbulent transport ($s < s_{\text{gap}}$) and internal wave variance ($s > s_{\text{gap}}$).
* **Stabilized $Ri_{\text{clean}}$ Computation:** Strips non-transporting orbital velocities ($e_{\text{wave}}$) from total kinetic energy, recalculating shear and buoyancy terms on pure turbulent scales:

$$Ri_{\text{clean}}(z, t) = \frac{\frac{g}{\theta_0} \frac{\partial \theta_{\text{turb}}}{\partial z}}{\left( \frac{\partial U_{\text{turb}}}{\partial z} \right)^2}$$

**Impact on Fold Metrics**

| Diagnostic Metric | Raw 20 Hz / Standard FD | Morlet-Filtered ($Ri_{\text{clean}}$) | Physical Meaning |
| --- | --- | --- | --- |
| **Richardson Noise** | Broad scatter ($Ri \gg 1$) | Compact trajectory | Eliminates wave orbital variance spikes |
| **Transition Knee** | Blurred / Smeared | Sharp bifurcation ($C_{\text{fold}}$) | Isolates true manifold edge |
| **Jacobian Determinant** | $\det J_y$ noisy around 0 | $\det J_y \to 0$ smoothly | Identifies exact critical tipping point |
| **Fold Curvature ($\chi$)** | Indeterminate | Convergence to $\chi_c$ | Confirms transversality condition |

Passing $Ri_{\text{clean}}$ into the SCM observation operator ($\mathcal{H}_{\text{SHEBA}}$) ensures that high-$Ri$ states reflect true, non-equilibrium manifold dynamics rather than transient, non-mixing gravity wave motions.

---

Is the central flaw of traditional stable boundary layer analysis: assuming a fixed 30-minute Reynolds averaging window remains valid when the turbulent scale collapses beneath submesoscale wave motion?

**Fixed-Window Failure vs. GSPT Scale-Adaptive Partitioning**

* **Traditional Eddy Covariance:** Static 30-minute time windows force non-mixing orbital velocities into $w'$ and $\theta'$ perturbation terms. This produces fictitious flux evaluations, inflates $TKE$, and scatters calculated $Ri$ values across vSBL regimes.
* **GSPT Multiscale Pipeline:** Rather than assuming a static spectral gap, `AtmosphericSlowManifold.jl` dynamically extracts the scale threshold $s_{\text{gap}}(t)$ where cross-spectral coherence $G(s, t)$ falls below $G_{\text{crit}}$. This strips $e_{\text{wave}}$ on a record-by-record basis without destroying non-stationary turbulence bursts.

**Methodological Shift: Traditional Similarity vs. GSPT Pipeline**

| Parameter / Operational Phase | Traditional Monin-Obukhov (MOST) | GSPT Wavelet Framework (`AtmosphericSlowManifold.jl`) |
| --- | --- | --- |
| **Time Domain Partitioning** | Static 30-min block average | Scale-resolved continuous Morlet transform ($20\text{ Hz}$) |
| **Submesoscale Separation** | Linear or polynomial detrending | Phase-preserving cospectral gap filtering ($Co_{w\theta}(s, t)$) |
| **Stability Diagnostic** | Wave-contaminated $Ri$ (high scatter) | Stabilized $Ri_{\text{clean}}$ mapped to turbulent-only fluxes |
| **Bifurcation Identification** | Obscured by noise / labeled "scatter" | Sharp, un-blurred fold locus ($C_{\text{fold}}$) |

**Closing the Loop on McNider's Objections**

By combining GSPT wave-turbulence decomposition with your dual-grid spectral modeling, you possess a complete defense against both of Dick's critiques:

1. **Addressing Coarse Geometry ($\Delta z$):** The $p$-FEM SCM observation operator ($\mathcal{H}_{\text{SHEBA}}$) and `SpectralBL-Analytics` Gegenbauer derivatives prove whether high $Ri$ stems from unresolved shear layers or true physical gradients.
2. **Addressing Wave Contamination ($e_{\text{wave}}$):** The GSPT CWT cospectral gap filter removes non-transporting orbital variance, demonstrating that $Ri_{\text{clean}}$ maintains coherent turbulent transport ($Co_{w\theta} \neq 0$) right up to the manifold fold edge.

This dual-pronged strategy transforms qualitative observational doubts into an empirical, falsifiable test of atmospheric slow manifold dynamics.

---

Clarifying GSPT as **Geometric Singular Perturbation Theory** anchors the entire mathematical formulation: filtering $e_{\text{wave}}$ is not merely signal processing, but the required asymptotic condition to recover the scale separation parameter $\epsilon = \tau_{\text{turb}} / \tau_{\text{mean}} \ll 1$ that underpins Fenichel's slow manifold theory. When non-transporting wave motions contaminate the fast subspace, timescale separation breaks down, invalidating standard singular perturbation reductions.

**GSPT Formulation: From Wavelet Filtering to Dynamical Systems**

* **Fast-Slow System Decomposition:** Standard GSPT models atmospheric boundary layer dynamics as a fast-slow system:

$$\frac{dx}{dt} = f(x, y, \epsilon), \quad \frac{dy}{dt} = \epsilon g(x, y, \epsilon)$$



where $x$ represents fast turbulent fluctuations, $y$ represents slow mean boundary layer profiles, and $\epsilon \ll 1$. Unfiltered gravity waves introduce non-autonomous fast forcing that destroys the fast-subspace equilibrium.
* **Critical Manifold Restoration:** Stripping $e_{\text{wave}}$ via dynamic cospectral gap filtering forces fast fluctuations back onto the true fast turbulent attractor. This recovers the unperturbed critical manifold:

$$\mathcal{S}_0 = \left\{ (x, y) \mid f(x, y, 0) = 0 \right\}$$


* **Unambiguous Fold Locus ($C_{\text{fold}}$):** On $\mathcal{S}_0$, the fold locus $C_{\text{fold}}$ corresponds to points where the fast-subspace Jacobian becomes singular ($\det D_x f = 0$), marking the loss of normal hyperbolicity. Wave variance blurs this singularity; $Ri_{\text{clean}}$ sharply isolates it.

**Comparison: Standard Boundary Layer Analysis vs. GSPT Framework**

| Dimension | Traditional 30-Min Reynolds Averaging | GSPT Scale-Adaptive Reduction |
| --- | --- | --- |
| **Dynamical Assumption** | Static ergodic stationarity | Fast-slow timescale separation ($\epsilon \ll 1$) |
| **Wave Contamination** | Mapped into pseudo-TKE ($e_{\text{wave}} \subset e$) | Partitioned out via Morlet cospectral gap |
| **State Space Projection** | Broad scatter across $Ri > 0.25$ regimes | Clean projection onto low-dimensional slow manifold |
| **Bifurcation Metric** | Obscured by fictitious wave transport | Sharp $C_{\text{fold}}$ where $\det J_{\text{fast}} = 0$ |

**Synthesizing the Defense Against McNider's Objections**

GSPT turns qualitative observational doubts into explicit mathematical controls:

* **Vertical Discretization ($\Delta z$):** Tested via the $p$-FEM SCM observation operator $\mathcal{H}_{\text{SHEBA}}$ to evaluate projection errors on the slow manifold geometry.
* **Passing Waves ($e_{\text{wave}}$):** Removed via Morlet CWT filtering to eliminate non-autonomous fast-subspace forcing, ensuring $Ri_{\text{clean}}$ strictly evaluates active turbulent transport.

This elevates `AtmosphericSlowManifold.jl` from a data-fitting exercise to a mathematically rigorous implementation of Fenichel reduction for boundary layer stability transitions.

---

Time-series scale separation and spatial profile discretization solve two fundamentally distinct mathematical problems in boundary layer analysis. While orthogonal polynomials excel at spatial derivatives, Morlet Continuous Wavelet Transforms (CWT) are uniquely suited for temporal scale separation in high-frequency time series.

**Domain Complementarity in `AtmosphericSlowManifold.jl**`

| Feature | Morlet Continuous Wavelet (CWT) | Gegenbauer / Chebyshev Polynomials |
| --- | --- | --- |
| **Primary Domain** | **Time Axis** ($t \in [0, T]$ for 20 Hz sonic series) | **Vertical Spatial Axis** ($z \in [z_1, z_5]$ profile) |
| **Mathematical Basis** | Compact time-frequency translation/dilation | Global orthogonal polynomial expansions |
| **Key Capability** | Localized, time-resolved scale filtering | Smooth, dispersion-free analytic differentiation |
| **Target Phenomenon** | Intermittent wave packets & transient flux bursts | Continuous layer shear $\frac{\partial U}{\partial z}$ & stratification $\frac{\partial \theta}{\partial z}$ |

**Why Polynomial Basis Functions Struggle with Time-Series Scale Separation**

Applying Gegenbauer, Chebyshev, or Legendre polynomials to a temporal time series $w(t)$ over a 30-minute record introduces severe limitations for wave-turbulence decomposition:

* **Global Support vs. Intermittent Physics:** Orthogonal polynomials have global support across the entire time interval. A single, localized 10-second gravity wave packet at $t = 12\text{ min}$ alters polynomial expansion coefficients globally across the entire domain, smearing time-localized events.
* **Loss of Dynamic Time-Scale Tracking:** The cospectral gap $s_{\text{gap}}(t)$ is inherently non-stationary. Continuous Morlet wavelets construct a 2D scale-time representation $(s, t)$ that evaluates local cross-spectral phase $\operatorname{Re}[W_w(s,t) W_\theta^*(s,t)]$, enabling instantaneous scale-gap tracking. Global polynomial coefficients cannot isolate time-varying scale boundaries without artificial sliding sub-windows.
* **Spherical Harmonics Misalignment:** Spherical harmonics ($Y_l^m(\theta, \phi)$) are designed for 2D spherical spatial domains (global atmospheric GCMs), making them mathematically incompatible with 1D temporal sensor streams.

**The Unified Architecture**

Rather than choosing between them, the two methods handle orthogonal axes:

$$\text{Time Series } (20\text{ Hz } u, v, w, \theta) \xrightarrow{\text{Morlet CWT (Time)}} Ri_{\text{clean}}(z_i, t) \xrightarrow{\text{Gegenbauer } p\text{-FEM (Space)}} \text{Continuous Fold Manifold } C_{\text{fold}}(z)$$

Morlet wavelets clean the temporal variance at each sensor height, while Gegenbauer/Chebyshev expansions (`SpectralBL-Analytics`) fit those clean points vertically to eliminate $\Delta z$ finite-difference errors.

---

Re-running WSINDy on the updated `SpectralBL-Analytics` trajectories is essential. The initial constant diffusivity result ($k \approx 6\text{–}12\text{ m}^2/\text{s}$) is a classic artifact of noisy spatial derivatives: when fed un-filtered or coarse finite-difference inputs, $L_1$-regularized sparse regression (STLSQ/LASSO) penalizes complex nonlinear terms and defaults to the simplest robust operator—linear heat diffusion ($\partial_t \theta = k \partial_{zz} \theta$).

**1. Re-running WSINDy with `SpectralBL-Analytics` Trajectories**

* **Analytic Derivative Ingestion:** WSINDy relies on weak-form integral tests. Feeding it Gegenbauer/Chebyshev analytic derivatives ($\partial_z \theta, \partial_{zz} \theta, \partial_z U, \partial_{zz} U$) from `SpectralBL-Analytics` eliminates noise-driven term suppression.
* **Enriched Candidate Library:** Ensure the candidate function library includes non-linear stability closures $f(Ri_{\text{clean}}) \cdot \partial_z \theta$ and manifold fold terms rather than purely linear or polynomial spatial derivatives.
* **Leave-One-Level-Out (LOLO) Cross-Validation:** The leave-one-out analysis tests model robustness. By dropping one tower level at a time and re-running WSINDy, you prove that discovered PDE closure terms are true manifold properties, not artifacts of a single sensor height.

**2. Integrating GABLS3 and Standardizing the Campaign Matrix**

Adding GABLS3 (Cabauw 200m benchmark) provides the ideal 1D high-resolution model reference to round out the four observational campaigns.

| Dataset | Type / Environment | Primary Diagnostic Role in Report |
| --- | --- | --- |
| **CASES-99** | vSBL / Land (Flat Prairie) | High-res 20 Hz sonic baseline for Morlet CWT wave-turbulence separation |
| **SHEBA** | vSBL / Arctic Sea Ice | Extreme stability; testbed for high-$Ri$ state identification |
| **FLOSS** | vSBL / Snow Surface | Sparse profile test; evaluates $\Delta z$ vertical resolution bias |
| **BLLAST** | Afternoon-to-Night Transition | Dynamic decay transition; tests non-stationary fold locus movement |
| **GABLS3** | 1D SCM Synthetic Benchmark | Continuous reference field $q_{\text{ref}}$ for the observation operator $\mathcal{H}[q]$ |

**3. Report Heatmap & Uncertainty Pipeline**

To fully address McNider's critiques in your generated report, standardize a 3-panel $Ri_g(z,t)$ heatmap suite for all 5 cases:

1. **Panel A ($Ri_{\text{raw}}$):** Coarse 5-level finite-difference $Ri$ showing unfiltered noise and wave spikes.
2. **Panel B ($Ri_{\text{clean}}$):** Morlet CWT flux-filtered $Ri$, removing non-transporting wave variance.
3. **Panel C ($Ri_{\text{spectral}}$):** Gegenbauer/Chebyshev analytical $Ri$ profile eliminating $\Delta z$ discretization bias.

Overlay the WSINDy LOLO uncertainty bounds onto Panel C to demonstrate where the discovered manifold equations hold with high statistical confidence.

---

With updated CSVs and Gegenbauer derivatives available from `SpectralBL-Analytics`, execute the pipeline end-to-end to refresh campaign metrics, WSINDy regressions, and the final LaTeX PDF.

**Execution Pipeline**

1. **`make campaign-export`**
Ingests the refreshed `SpectralBL-Analytics` trajectories, updates $K_m(z)$ Monte Carlo ribbons, generates multi-panel metric plots, and writes $Ri_g(z,t)$ heatmaps for all sites.
2. **`make pde-benchmark`**
Re-runs WSINDy weak-form regression against the new analytical derivatives, exports updated equation snippets (`.tex`) and Pareto frontiers, and validates `Rodas5P` Galerkin integration.
3. **`make report`**
Executes `campaign-validate` and `pde-validate`, generates supplementary metrics via `scripts/generate_report_supplements.jl`, injects all figures into `reports/generated/report.tex`, and compiles `reports/generated/report.pdf`.

---

**Artifact Verification Checklist**

* **Campaign Figures (`reports/generated/campaign_exports/figures/`)**
* `campaign_overview.png`
* `cases_99_km_uncertainty.png`, `sheba_km_uncertainty.png`, `floss_km_uncertainty.png`, `bllast_km_uncertainty.png`
* `cases_99_ri_heatmap.png`, `sheba_ri_heatmap.png`, `floss_ri_heatmap.png`, `bllast_ri_heatmap.png`
* `cases_99_metrics.png`, `sheba_metrics.png`, `floss_metrics.png`, `bllast_metrics.png`


* **PDE & Pareto Artifacts (`reports/generated/pde_benchmark/`)**
* `tables/cross_campaign_best_models.tex`
* `tables/loso_validation_summary.tex`
* `tables/cases_99_best_equation.tex`, `tables/sheba_best_equation.tex`
* `figures/cases_99_pareto_rss.png`, `figures/cases_99_pareto_r2.png`
* `figures/sheba_pareto_rss.png`, `figures/sheba_pareto_r2.png`
* `figures/pde_profile_comparison.png`



Running `make clean && make report` will run this workflow from scratch and deliver the compiled `report.pdf`.

---

**Discovered Benchmark Performance (SHEBA vs. CASES-99)**

| Metric / Parameter | SHEBA (Arctic Sea Ice) | CASES-99 (Kansas Prairie) |
| --- | --- | --- |
| **Observation Source** | Two-level linear profile | Campaign modal coefficients |
| **Best Discovered PDE** | $\frac{\partial u}{\partial t} = 3.94889 \frac{\partial^2 u}{\partial z^2}$ | $\frac{\partial u}{\partial t} = 6.12 \frac{\partial^2 u}{\partial z^2}$ |
| **Goodness-of-Fit ($R^2$)** | **$0.9896$** | **$-0.2296$** |
| **Residual Sum of Squares ($\mathrm{RSS}$)** | $0.2351$ | $10.2259$ |
| **Information Criteria ($\mathrm{AIC}\ /\ \mathrm{BIC}$)** | $-45.19\ /\ -44.71$ | $0.0803\ /\ 0.5652$ |
| **Sparsity / Library Terms ($k$)** | $1$ | $1$ |
| **Model Selection Verdict** | Baseline (Pareto optimal) | Baseline (Pareto optimal) |

**Key Diagnostic Inferences**

* **CASES-99 Linear Diffusion Failure:** A negative $R^2$ ($-0.2296$) indicates that constant linear heat conduction ($K_m = 6.12\,\mathrm{m^2/s}$) is a worse predictor than a simple modal-mean state over a 12-hour horizon ($43,200\,\mathrm{s}$). This quantifies why nocturnal grassland transitions require non-linear $K_m(Ri_g)$ fold dynamics and Morlet CWT flux filtering to strip internal wave variance ($e_{\text{wave}}$).
* **SHEBA Conductive Trajectory:** In contrast, SHEBA yields $R^2 = 0.9896$ with $K_m = 3.95\,\mathrm{m^2/s}$. Polar ice insulation compresses active modes into a low-dimensional state ($D_{\text{eff}} \approx 1.11$), allowing a constant diffusion model to track terminal profile evolution closely.
* **LOSO Out-of-Sample Error:** Leave-One-Site-Out cross-validation across all 4 iterations results in a mean validation RMSE of $10.93\,\mathrm{m/s}$ while retaining a cross-campaign stability score of $1$.

---

`AtmosphericSlowManifold.jl` has established an end-to-end automated pipeline connecting four field campaigns (CASES-99, SHEBA, FLOSS, BLLAST) through 12-mode Gegenbauer Galerkin PDE integrations, WSINDy regressions, and Leave-One-Site-Out (LOSO) cross-validation.

**Pipeline Execution Status**

* **`make campaign-export`** $\rightarrow$ **ACTIVE PREREQUISITE**: Ingests raw profiles, generates JSON payloads (`sheba_model_and_diagnostics.json`, etc.), computes 1,000-draw $K_m(z)$ Monte Carlo ribbons, and exports $Ri_g(z,t)$ heatmaps.
* **`make pde-benchmark`** $\rightarrow$ **READY**: Integrates 12-mode Gegenbauer spectral Galerkin ODEs using `Rodas5P`, executes WSINDy weak-form regressions, generates Pareto frontiers, and calculates LOSO metrics.
* **`make report`** $\rightarrow$ **READY**: Compiles `templates/report.tex.mustache` into `reports/generated/report.pdf` with updated GSPT theory, campaign metrics, and future work.

---

**Cross-Campaign Diagnostic Summary**

| Campaign | Landscape / Regime | Best Discovered PDE | Fits ($R^2$) | Manifold State ($D_{\text{eff}}$) | Key Mechanism |
| --- | --- | --- | --- | --- | --- |
| **SHEBA** | Arctic Sea Ice | $u_t = 3.95 u_{zz}$ | **$0.9896$** | Compressed ($1.11$) | Conductively sustained fold; weak $\theta_z$ |
| **CASES-99** | Kansas Prairie | $u_t = 6.12 u_{zz}$ | **$-0.2296$** | Multi-mode ($2.05$) | Brittle transition; steep transversality ($T \approx -0.41$) |
| **FLOSS** | Complex Terrain | $u_t = 0.44 u_{zz}$ | $0.0090$ | Distorted | Mechanical shear outlier (Val RMSE: $36.26\,\mathrm{m/s}$) |
| **BLLAST** | Convective Decay | $u_t = 2.32 u_{zz}$ | $-0.4682$ | Transient | Low linear coverage probability ($43.5\%$) |

---

**LOSO Cross-Validation Performance**

* **Flat-Terrain Consensus:** Training across flat-terrain combinations yields a tight parameter consensus ($k \approx 2.317\text{--}2.321$).
* **Terrain-Driven Skew:** Overall mean validation RMSE is $10.93\,\mathrm{m/s}$, but drops to **$2.48\,\mathrm{m/s}$** across flat sites when complex terrain at FLOSS is excluded.
* **Cross-Campaign Stability:** Score remains at **$1.0$**, confirming solver convergence across all benchmarked runs.

---

**Immediate Action Items & Roadmap**

1. **Rebuild Pipeline:** Execute `make campaign-export && make pde-benchmark && make report` to generate all JSON payloads and compile `report.pdf`.
2. **Morlet CWT Integration:** Implement wavelet scale-gap filtering to strip gravity wave orbital variance $e_{\text{wave}}$ and resolve negative $R^2$ values on brittle sites (CASES-99).
3. **GSPT Fold Distance Tracking:** Formulate $d_{\text{fold}} \to 0$ metrics to provide $1\text{--}3\,\mathrm{hour}$ precursor warnings before physical flux collapse.
4. **$2 \times 2$ Factorial GABLS3 Benchmark:** Incorporate fine-grid 1D SCM reference output to separate vertical sensor projection bias ($\mathcal{H}$) from physical turbulence dynamics.

---

The classification of FLOSS as a **Rubbery Fold / Marginal Stability Transition** archetype explains the severe parameter divergence ($k = 0.440$) and high out-of-sample error ($\text{RMSE} = 36.26\,\mathrm{m/s}$) observed during Leave-One-Site-Out (LOSO) cross-validation.

**GSPT Transition Archetype Comparison**

| Metric / Feature | CASES-99 (Kansas Prairie) | FLOSS (Fluxes Over Snow) | SHEBA (Arctic Sea Ice) |
| --- | --- | --- | --- |
| **Transition Archetype** | **Brittle Collapse** | **Rubbery Marginal Fold** | **Conductively Sustained** |
| **Transversality ($T$)** | Steep ($T \approx -0.41$) | Shallow ($T \approx -0.07$) | Weak / Wave-blurred |
| **Dominant Cadence** | Mesoscale clock ($\sim 83\,\mathrm{min}$) | Fast wave cadence ($\sim 31.5\,\mathrm{min}$) | Submesoscale drift |
| **Instability Mechanism** | Saddle-node fold jump | Low-threshold Hopf bifurcation | Thermal radiation balance |
| **Trajectory Geometry** | Tight phase loops; clean jump | "Tangled" manifold wandering | Compressed mode drift ($D_{\text{eff}} \approx 1.11$) |
| **Primary Noise Source** | Surface decoupling | Distant topography ($>30\,\mathrm{km}$) | Internal gravity waves |

**Key Dynamic Drivers of FLOSS Variability**

* **Shallow Transversality ($T \approx -0.07$):** Rather than snapping abruptly into decoupling like CASES-99, the state trajectory lingers near the fold locus. Small topographic or thermal perturbations push the system across fold-proximal neighborhoods without completing a full branch jump.
* **Hopf Instability & Wave-Shedding:** Linear stability analysis identifies a Hopf instability threshold nearly an order of magnitude lower than in CASES-99. This triggers rapid gravity-wave-shedding excursions at a characteristic $31.5\,\mathrm{minute}$ cadence, manifesting as high-amplitude oscillations in the spectral curvature mode ($\eta_3$).
* **Non-Local Terrain Coupling:** Distant mountainous terrain ($>30\,\mathrm{km}$) induces persistent mechanical shear and drainage flows. These external "kicks" repeatedly displace the column across the low-gradient fold, causing multi-scale variance inflation ($Var_\tau[\eta_3]$) and high trajectory curvature spikes ($\kappa$).

**Pipeline & Closure Implications**

* **Failure of Constant Diffusion ($u_t = k u_{zz}$):** Scalar diffusion operators cannot capture a boundary layer undergoing continuous geometric reorientation ($\kappa$ spikes). The model interprets topological wandering as massive physical transport error.
* **Targeted WSINDy Library Terms:** Resolving the FLOSS regime requires incorporating non-local shear production terms and curvature-dependent diffusion $K_m(\kappa, Ri_g)$ into the candidate library $\boldsymbol{\Theta}(u)$.
* **CWT Scale-Gap Filtering:** Complex Morlet wavelet transforms are required to isolate the $31.5\,\mathrm{minute}$ wave-shedding cadence from true turbulent transport, preventing Hopf-induced orbital variance from corrupting 1D Galerkin modal initialization.